#include "user_lib.h"
#include "util/types.h"

// extern process user_app;

int main(void) {
  // gethartid();
  printu(">>> app0 is expected to be executed by hart0\n");
  // int cpuid = user

  exit(0);
}